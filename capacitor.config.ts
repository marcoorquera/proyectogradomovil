import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.muebleria.global',
  appName: 'muebleriaApp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
